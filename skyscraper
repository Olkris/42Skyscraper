#include <stdlib.h>
#include <unistd.h>
#include <stdio.h>

int  *hardrules(int a, int b)
{
        int     *val;

        val = malloc (4 * sizeof(int));
        if ((a == 1) && (b == 4)) val[] = {4, 3, 2, 1};
        if ((a == 4) && (b == 1)) val[] = {1, 2, 3, 4};
        if ((a == 1) && (b == 2)) val[] = {4, 0, 0, 3};
        if ((a == 2) && (b == 1)) val[] = {3, 0, 0, 4};
        if ((a == 2) && (b == 3)) val[] = {0, 4, 0, 0};
        if ((a == 3) && (b == 2)) val[] = {0, 0, 4, 0};
        return (val);
}

int     checkerror(int a, int b)
{
        int     i;

        i = 1;
        if ((a == 4) && (b != 1)) i = 0;
        if ((b == 4) && (a != 1)) i = 0;
        if ((a == 1) && (b == 1)) i = 0;
        if ((a == 3) && (b == 3)) i = 0;
        return (i);
}


int     *ft_sky_scraper(int *in)
{
        int     row;
        int     col;
        int     skynum;

        col = 0;
        row = 8;
        while ((col < 4) && (row < 12))
        {
                if (checkerror(in[col], in[col+4]) == 0) || (checkerror(in[row], in[row+4]) == 0)
                {
                        return(0);
                }
                else
                {
                        tab[4] = hardrules(in[col], in[col+4]);
                        tab[4] = hardrules(in[row], in[row+4]);
                        row++;
                        col++;
                }
        }
        return
}



int     main(void)
{
        int     *tab;
        int     i;

        tab = malloc (4 * sizeof(int));
        tab = hardrules(1, 4);
        i = -1;
        while (i++ < 3) printf("%d ", tab[i]);
        free(tab);
        return (0);

}

